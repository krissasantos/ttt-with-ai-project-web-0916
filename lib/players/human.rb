module Players
  class Human < Player
    # Players::Human
    #this argument will not be used

    # def move(board=[]) 
    def move(board)
       # x = STDIN.gets
       # ARGV.clear
       # STDIN.gets.chomp()
        gets.chomp #ENOENT ERROR!!!!!!!

      # $stdin.gets
    end
  end
end